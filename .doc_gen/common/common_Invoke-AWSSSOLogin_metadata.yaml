---
common_Invoke-AWSSSOLogin:
  title: Use Invoke-AWSSSOLogin
  title_abbrev: Invoke-AWSSSOLogin
  synopsis: use Invoke-AWSSSOLogin
  languages:
    PowerShell:
      versions:
      - sdk_version: 5
        excerpts:
        - description: '<emphasis role="bold">Example 1: Initiates the SSO login flow for the profile "sso-test-profile". The command will attempt to automatically open the SSO authorization page in the default web browser. Once the authentication process is complete, an AWS SSO access token is retrieved and cached, and can be exchanged for AWS credentials by passing the profile name to service cmdlets.</emphasis>'
          snippet_files: []
        - description: 
          snippet_files:
          - .doc_gen/snippet_files/common_Invoke-AWSSSOLogin.1.1.ps1
  services:
    common:
      Invoke-AWSSSOLogin: 
