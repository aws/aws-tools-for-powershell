<?xml version="1.0" encoding="utf-8"?>
<examples>
  <example>
    <code>Update-LMFunctionCode -FunctionName MyFunction -ZipFilename .\UpdatedCode.zip</code>
    <description>Updates the function named 'MyFunction' with new content contained in the specified zip file. For a C# .NET Core Lambda function the zip file should contain the compiled assembly.</description>
  </example>
  <example>
    <code>Update-LMFunctionCode -FunctionName MyFunction -BucketName mybucket -Key UpdatedCode.zip</code>
    <description>This example is similar to the previous one but uses an Amazon S3 object containing the updated code to update the function.</description>
  </example>
</examples>