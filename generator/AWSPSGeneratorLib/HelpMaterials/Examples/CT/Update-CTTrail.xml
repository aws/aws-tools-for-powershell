<?xml version="1.0" encoding="utf-8" ?>
<examples>
    <example>
        <code>Update-CTTrail -Name "awscloudtrail-example" -IncludeGlobalServiceEvents $true -S3KeyPrefix "globallogs"</code>
        <description>Updates the specified trail so that global service events (such as those from IAM) are recorded and changes the common key prefix of the log files going forwards to be 'globallogs'.</description>
    </example>
    <example>
        <code>Update-CTTrail -Name "awscloudtrail-example" -SnsTopicName "mlog-deliverytopic2"</code>
        <description>Updates the specified trail so notifications about new log deliveries are sent to the specified SNS topic.</description>
    </example>
    <example>
        <code>Update-CTTrail -Name "awscloudtrail-example" -S3BucketName "otherlogs"</code>
        <description>Updates the specified trail so logs are delivered to a different bucket.</description>
    </example>
</examples>
