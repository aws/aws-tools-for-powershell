<?xml version="1.0" encoding="utf-8"?>
<examples>
  <example>
    <code>Remove-ELBTags -LoadBalancerName my-test-loadbalancer -Tag @{ Key="project" }</code>
    <description>Removes the specified tag from the load balancer. You are prompted for confirmation unless the -Force switch is also specified.</description>
  </example>
  <example>
    <code>Remove-ELBTags -LoadBalancerName my-test-loadbalancer -Tag @{ Key="project" },@{ Key="othertagKey" }</code>
    <description>Removes the specified tags from the load balancer. You are prompted for confirmation unless the -Force switch is also specified.</description>
  </example>
</examples>