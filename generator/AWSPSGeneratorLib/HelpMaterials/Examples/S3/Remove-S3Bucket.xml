<?xml version="1.0" encoding="utf-8"?>
<examples>
  <example>
    <code>Remove-S3Bucket -BucketName test-files -DeleteBucketContent</code>
    <description>This command removes all objects and object versions from the bucket 'test-files' and then deletes the bucket. The command will prompt for confirmation before proceeding. Add the -Force switch to suppress confirmation. Note that buckets that are not empty cannot be deleted.</description>
  </example>
</examples>