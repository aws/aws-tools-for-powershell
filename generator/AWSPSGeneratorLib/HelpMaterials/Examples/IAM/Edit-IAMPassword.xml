<?xml version="1.0" encoding="utf-8"?>
<examples>
  <example>
    <code>PS C:\>Edit-IAMPassword -OldPassword "MyOldP@ssw0rd" -NewPassword "MyNewP@ssw0rd"</code>
    <description>This command changes the password for the user that is running the command.<br/>This command can be called by IAM users only. If this command is called when you are signed-in with AWS account (root) credentials, the command returns an <code>InvalidUserType</code> error.</description>
  </example>
</examples>
