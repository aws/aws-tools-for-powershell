<?xml version="1.0" encoding="utf-8" ?>
<examples>
    <example>
        <code>Write-KINRecord -Text "test data from string" -StreamName "mystream" -PartitionKey "Key1"</code>
        <description>Writes a record containing the string supplied to the -Text parameter.</description>
    </example>
    <example>
        <code>Write-KINRecord -FilePath "C:\TestData.txt" -StreamName "mystream" -PartitionKey "Key2"</code>
        <description>Writes a record containing the data contained in the specified file. The file is treated as a sequence of bytes so if it contains text, it should be written with any necessary encoding before using it with this cmdlet.</description>
    </example>
</examples>
