<?xml version="1.0" encoding="utf-8" ?>
<examples>
    <example>
        <code>Remove-ECSService -Service my-http-service</code>
        <description>Deletes the service named 'my-http-service' in the default cluster. The service must have a desired count and running count of 0 before you can delete it.<br/>You are prompted for confirmation before the command proceeds. To bypass the confirmation prompt add the -Force switch.</description>
    </example>
    <example>
        <code>Remove-ECSService -Cluster myCluster -Service my-http-service</code>
        <description>Deletes the service named 'my-http-service' in the named cluster.</description>
    </example>
</examples>
